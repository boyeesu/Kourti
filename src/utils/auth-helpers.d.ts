export function getAuthRedirectUrl(path: string, appUrl?: string): string;
export function buildDisplayName(
  firstName?: string | null,
  lastName?: string | null,
  email?: string | null,
): string;
