# Base URL of the Kourti backend service (the same service the app talks to).
# Locally this is the backend dev server; in production it's the backend's
# Railway public domain. The marketing site only calls /api/v1/public/*.
VITE_BACKEND_API_URL="http://localhost:4000"
